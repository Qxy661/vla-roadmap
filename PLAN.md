# 模块化执行计划（完整版）

> 主线：移动机器人 + VLA 大脑。节奏：全职冲刺 16–18 周。
> 落盘日期：2026-08-01。所有仓库推到 GitHub Qxy661。

---

## 一、总览：1 条主链 + 1 条支线

```
主链（串行，每步有前置依赖）:
  M0 环境 → M1 深度学习 → M2 YOLO全流程 → M3 VLA核心
         → M4 ROS2+仿真小车 → M5 SLAM+路径规划 → M6 控制对接
         → M8 端到端集成Capstone

支线（并行，可随时插入）:
  M7 无人机（PX4仿真）  ← 主链到 M4 后可随时并行走
```

| 周次 | 主链 | 支线 |
|---|---|---|
| 1 | **M0** 环境基建 + starVLA 快速上手（先导） | |
| 2–3 | **M1** 深度学习核心（PyTorch 手搓） | |
| 4–5 | **M2** YOLO 全流程闭环 | |
| 6–8 | **M3** VLA 核心动手（starVLA 微调） | |
| 9–11 | **M4** ROS2 + 仿真小车 | 并行读 PX4 文档 |
| 12–14 | **M5** SLAM + 路径规划 | **M7** PX4 SITL 仿真飞行 |
| 14–15 | **M6** 控制层对接 | |
| 16–18 | **M8** 端到端集成 | |

> M7 是加分项，不阻塞主线；精力不够可砍，不影响全流程能力成型。

---

## 二、模块详细卡片

### 模块 0：环境与基建（1 周）

**能力目标**：一键拉起整个开发环境，任何模块依赖不卡壳。

**子任务**：
1. 验证 WSL2 已在 **D 盘**（已确认：`D:\wsl\Ubuntu-22.04`，Ubuntu 22.04，WSL 2.6.3）→ 无需迁移
2. Python 深度学习环境（miniconda + PyTorch + CUDA 验证）
3. ROS2 Humble 完整安装（Gazebo、Nav2、rviz2、slam_toolbox）
4. 克隆 starVLA，跑通 Quick Start + 现成 checkpoint 推理（先导激励）
5. 确认 AutoDL 账号 / 云 GPU 方案（VLA 微调用）

**实操项目**：环境一键搭建脚本 `setup.sh` + 每个工具 helloworld。

**交付物**：`vla-workspace/`（环境脚本 + 安装验证清单）。

**验收标准**：口述"我在哪台机子上用什么版本跑什么"，且能白手起家复现全流程安装。

**关键决策**：ROS2 用 **Humble + Ubuntu 22.04**，不用 Jazzy/24.04——Nav2、Gazebo、slam_toolbox 的教程与二进制兼容最成熟。

**踩坑预警**：WSL2 vhdx 默认放 C 盘会爆盘 → 你已装 D 盘（`D:\wsl\Ubuntu-22.04`），此坑已避开。注意别把大型数据（数据集/模型/conda 缓存）堆进 C 盘（仅剩 18.5GB）。

---

### 模块 1：深度学习核心（2 周）

**能力目标**：不看文档写出完整训练循环；能解释张量、自动求导、反向传播、过拟合。

**子任务**：
1. PyTorch：张量、autograd、Dataset/DataLoader、训练循环、验证
2. 手搓 MLP → CNN，**手写一次反向传播**（不靠 torch.autograd）
3. CIFAR-10 分类完整闭环（训练曲线/混淆矩阵/可视化）
4. 读懂 ViT 视觉编码器结构——通向 VLA 感知的桥，标注"CNN 检测 vs Transformer 视觉"的差异

**实操项目**：`dl-hands-on/`——手搓 CNN 在 CIFAR-10 达到目标精度，TensorBoard 全程记录。

**交付物**：可复现仓库 + 反向传播手写笔记。

**验收标准**：口述"图片从像素到类别概率"的全过程；能闭眼写训练循环骨架。

**运行位置**：本地（RTX 4060 足够）。

---

### 模块 2：YOLO 全流程闭环（2 周）

**能力目标**：掌握"数据→标注→训练→评估→部署"完整闭环。**重点在流程**，不深挖精度。

**子任务**：
1. 自建小数据集（采集/下载 500–2000 张 + 标注，LabelStudio/Roboflow）
2. ultralytics YOLOv8 训练 + 精度分析 + 一轮调参
3. 导出 ONNX → TensorRT，webcam 实时推理
4. 对照 YOLO 检测头 vs ViT 视觉编码器——理解检测与 VLA 感知的分野

**实操项目**：`yolo-pipeline/`——实时检测桌面物体（这组物体后续给 Capstone 感知层用，是"未来资产"）。

**交付物**：可复现训练脚本 + 导出部署脚本 + 演示视频。

**验收标准**：自训模型实时跑通；能独立新增一类物体走一遍全流程。

**运行位置**：本地。

---

### 模块 3：VLA 核心动手（3 周）← 题眼

**能力目标**：读懂任意 VLA 论文架构、亲手微调一个模型、理解数据层真实复杂度。

**子任务**：
1. 解剖 starVLA：backbone（Qwen/Florence-2/InternVL）× action head 三大流派（FAST 离散 token / PI 流匹配 / GR00T 双系统）
2. 横向对比 RT-2 / π0 / OpenVLA / ACT，画"设计空间地图"（离散 vs 连续、单 stream vs 双 system、是否扩散）
3. 用 starVLA 在 **LIBERO 仿真**上微调模型并跑评测
4. 啃数据层：RLDS 格式、`docs/integrate_your_dataset.md`、遥操作原理、数据增强
5. 进阶（可选）：RL 后训练 / WM4A

**实操项目**：你微调的 VLA 在 LIBERO 完成指定任务，记录训练曲线与评测得分。

**交付物**：`vla-starvla-study/`——架构解剖笔记 + 可复现微调配置 + 评测结果表。

**验收标准**：能对着任意 VLA 代码说出"哪个是 backbone、哪个是 action head、动作怎么解码"；能独立给 starVLA 接入自定义格式数据集。

**运行位置**：微调在 **AutoDL 云 GPU**（4090/A100，按小时）；解剖与推理本地可做。

---

### 模块 4：ROS2 + 仿真小车（3 周）

**能力目标**：独立搭建一台仿真移动机器人并驱动它。

**子任务**：
1. ROS2 核心：node/topic/service/action、tf2、launch、bag、rviz2
2. URDF/Xacro 搭**履带差速底盘**（相机、IMU、2D 激光）
3. Gazebo 仿真：物理引擎、里程计、传感器噪声
4. teleop → cmd_vel 驱动小车跑起来
5. 写第一个自己的 ROS2 功能包（发布/订阅/服务）

**实操项目**：`robot-sim-stack/`——仿真履带车底盘包 + 传感器数据流 + 自研节点。

**交付物**：URDF 小车 + launch 文件 + 自己的 ROS2 功能包。

**验收标准**：rviz 里看到小车在动、激光/相机话题实时刷新；能解释"指令从键盘到车轮的完整链路"。

**运行位置**：WSL2（Ubuntu 22.04）。

---

### 模块 5：SLAM + 路径规划（3 周）

**能力目标**：建图→定位→规划→导航闭环，**且手写过规划器**。

**子任务**：
1. 2D SLAM：slam_toolbox（先）→ Cartographer（可选加深）
2. AMCL 定位 + 占用栅格地图原理
3. **手写 A\***（Python → ROS2 节点，在栅格地图上跑）
4. **手写 RRT\*/DWA**，与经典算法对比
5. Nav2 栈：全局/局部规划器、代价地图、行为树；对比手写 vs 现成

**实操项目**：`path-planning/`——手写 A\*/RRT\* 独立成库 + 仿真小车用 Nav2 自主导航避障。

**交付物**：手写算法库 + Nav2 配置 + 自主导航演示。

**验收标准**：rviz 点目标点，小车自主规划、避障、到达；能口述 A* 与 RRT 的适用场景差异。

---

### 模块 6：控制层对接（1–2 周，很短）

**能力目标**：把"规划输出"真正变成"执行器动作"，复用历史资产。

**子任务**：
1. 复用已有 PID/MPC 代码做**轨迹跟踪**，仿真调参（Simulink 能力的迁移验证）
2. micro-ROS 原理 + 虚拟 MCU 接入 ROS2（未来真车换硬件）
3. 打通链路：规划 → 控制器 → 底盘执行

**实操项目**：轨迹跟踪控制器封装包 + 调参实验记录。

**交付物**：`control-deploy/`。

**验收标准**：小车跟踪给定轨迹、误差可控；能说清"哪层是 ROS2、哪层是控制器、哪层是执行器"。

---

### 模块 7：无人机（2 周，支线）

**能力目标**：PX4 仿真飞行 + 飞控对接入门（复用 PX4/ArduPilot 背景）。

**子任务**：
1. PX4 SITL + Gazebo 仿真搭建
2. QGC / MAVROS 对接
3. offboard 控制：起飞 → 悬停 → 航点飞行
4. 对比轮式栈 vs 空栈差异（控制频率、规划维度、仿真工具）

**实操项目**：`px4-sim/` 仿真无人机执行航点任务。

**验收标准**：仿真里起飞 → 航点 → 降落全自动。

---

### 模块 8：端到端集成 Capstone（3 周）← 能力总验收

**能力目标**：独立把语言指令变成真实执行，验证 8 个模块能力的缝合。

**子任务**（场景二选一或都做）：
- **场景 A（移动主线）**："把车开到柱子旁"——语言+相机 → **VLA 高层决策**（目标点/意图）→ SLAM 地图 → 路径规划 → 控制 → 执行
- **场景 B（机械臂操纵）**：LIBERO 桌面臂 + **YOLO 检测** → **VLA 动作块** → 执行

**实操项目**：`e2e-demo/` 端到端 demo + **全流程复盘文档**（从零到端到端的工程复盘）。

**验收标准**：完整任务从指令到执行全自动跑通；能讲清楚整套系统每一层在干什么、为什么这么设计。

---

## 三、环境技术方案（Windows 关键决策）

| 层 | 方案 | 说明 |
|---|---|---|
| 基础 | **WSL2 + Ubuntu 22.04**（已在 D 盘） | 你已有 WSL2 经验；另有 **Ubuntu 双系统（100G）** 留给未来真机原生跑 |
| ROS2 | Humble（Nav2 / Gazebo Classic / slam_toolbox） | 22.04 上二进制最全、教程最多 |
| 深度学习 | WSL2 内 conda + PyTorch | 本地（RTX 4060） |
| VLA 微调 | **AutoDL 云 GPU**（4090/A100，按小时） | 8GB 本地显存不够微调 |
| VLA 仿真 | starVLA（LIBERO/SimplerEnv 本就是仿真） | 不需要真机 |

**未来真机迁移**：接真车（USB 串口/电机驱动板）时 WSL2 的 USB 直通有摩擦 → 你已有 **Ubuntu 双系统（100G）**，届时直接切过去原生跑 ROS2。学习阶段继续用 WSL2（D 盘、空间充足、Windows 无缝）。跨系统交换用 Git/NTFS 共享，别直接读 ext4 分区。

---

## 四、全局铁律

1. **每个模块以"验收标准"收尾**，不是以"做完任务"收尾——口述自检 + 演示，过不了回炉。
2. **每个模块产出一个 GitHub 级仓库**（README + 可复现脚本 + 文档 + 演示），全推 Qxy661。
3. **文档随项目走**，不是事后补：每模块一篇技术笔记进 `references/`。
4. **主链串行、支线并行**：任何时刻只推进一个主链模块。
5. **每模块留"未来资产"**：M2 检测集、M4 小车包、M5 规划器 → Capstone 直接拼接。

---

## 五、启动条件

模块 0 在用户明确"开始"后启动。启动第一件事：**WSL2 装到 D 盘**（否则爆盘），然后按 M0 卡片顺序执行。
