# VLA 学习 · 会话记忆档案（可靠持久版）

> 这是跨会话记忆的**可靠持久记录**（因 Claude Code 记忆系统在本环境不可靠）。
> 任何会话开始，读此文件即可恢复完整上下文。
> 最后更新：2026-08-02

## 一、用户信息与偏好

- 昵称：小羊（文档展示用）
- 主力机：HP Victus，RTX 4060 Laptop 8GB / 32GB RAM
- 磁盘：C 剩 16GB / D 剩 20GB / E 剩 20GB（D 盘 steam 235GB 大头）
- WSL2：Ubuntu 22.04 在 D:\wsl\Ubuntu-22.04，conda 环境 `dl`
- 双系统：有 Ubuntu 100G（未来真机用）
- 语言：中文交流，英文代码注释，中文文档

## 二、学习主线与模块

### 主线：VLA × 自主移动机器人（非机械臂）

```
M1 深度学习基础 → M2 YOLO检测 → M3 VLA核心 → M4 机器人栈
```

### 各模块状态

| 模块 | 状态 | 项目位置 |
|---|---|---|
| M1 深度学习 | ✅ 完成 | `dl-hands-on`（手写引擎+CNN 80.5%）|
| M1C MiniTorch | ✅ 完成 | `minitorch`（纯NumPy框架 48.8%）|
| M1C2 二分类 | ✅ 完成 | `binary-classifier`（迁移学习89.5%/奶龙塔菲100%）|
| **M2 YOLO** | 🔄 进行中 | `E:\yolo-visdrone` |
| M3 VLA | ⏳ 规划好 | 待M2完成 |

### 关键决策

- **模块独立而衔接**，允许回炉/挖深
- **学习态/展示态分离**：教程(学习) + 工程文档(展示)
- **应用流程闭环** = 数据→训练→评估→部署，所有模块统一方法论
- 效果优先、冲数字、新旧不对比、多应用分支

## 三、M2 详情（当前模块）

- **项目**：`E:\yolo-visdrone`（GitHub Qxy661/yolo-visdrone）
- **主任务**：VisDrone 小目标检测，YOLO26s + 800px + 强增强训练中
- **训练状态**：✅ 完成（50/50 epochs）
  - **VisDrone 最终**：mAP50 0.382 / mAP50-95 0.219 / P 0.508 / R 0.389
  - 诊断：漏检为主（R低），bicycle/awning-tricycle最难（AP 0.13-0.14），car最好（0.79）
  - **SAHI 优化**：同阈值对比 mAP50 +27%（0.300→0.381）
  - **钢珠检测**：mAP50 0.93 / P 0.89 / R 0.88，测量+高光校验8/8验证
  - **部署**：ONNX导出（30ms/图）+ WebUI交互页（gradio）核心验证
  - README 重写为 YOLO 应用集主题，30提交待推送
- **知识体系**：docs/tutorial/ 7 篇完整（检测基础→YOLO原理→小目标→数据→训练→部署→闭环）
- **钢珠分支**：`applications/steel-ball/`，1943张数据+检测+直径测量+高光校验+EDA
- **作品集规范**：README(闭环框架)+CONTRIBUTING+LICENSE+requirements 完成

### M2 收尾任务
1. 训练评估（mAP/AP_small）
2. 钢珠训练（GPU空后，数据就绪）
3. 部署实操（ONNX/TensorRT）
4. 作品集定稿（填数字）

## 四、M3 规划（下一个模块）— 2026-08-03 宏图深化

- **定位**：上手实操 + 本地小模型 + 独立模块（M2 已全部完成，可启动）
- **现状核查**：starVLA 1.0.1 已装（/root/projects/starVLA），支持 Qwen3-VL 0.8/2/4/9B，有 LIBERO 例子
- **模型**：**Qwen3-VL-0.8B**（首选，starVLA 主推，8GB 可训练）或 Florence-2
- **路线**：5阶段（环境验证→数据理解→微调→评估→作品集），每阶段有核查点
- **验收**：本地跑通加载+推理/理解视觉+语言→动作/完成一次微调/独立模块
- **工作量**：预计 6-8 天
- **方案**：`references/m3-vla-plan.md`（宏图可核查版）

## 四.5 核心方法论（用户强调，长期指导）

**评估是独立核心能力，不只是调参**：
- 不看单一 mAP，综合 P/R/F1/AP_small/按类别/混淆矩阵诊断
- 先诊断（数据说话）再优化（针对痛点），不是盲调
- 小目标场景：漏检多（R低）是典型，用 SAHI/高分辨率/P2头/阈值平衡
- 按场景加权：电赛重 R（不漏检），部署重 P（少误检）
- 评估→诊断→优化的闭环思维，比具体参数更重要
- 落地文档：`docs/EVALUATION_METHODOLOGY.md`

## 五、关键事实（避免重复踩坑）

- 网络：WSL走代理，国外源慢；国内源(华为云/hf-mirror)直连快
- HF下载：git clone + git-lfs 最稳（绕过API 429限流）
- ROS污染：`scripts`包名冲突，用 importlib 加载
- numpy冲突：`/root/.local` 和 conda 混用 → 用 PYTHONPATH 指定 conda
- 训练慢：纯NumPy慢、imgsz 800 每epoch 3-4分钟

## 六、环境备忘

- WSL conda `dl` 环境：torch 2.6.0+cu124, ultralytics 8.4.115
- VisDrone数据：`/root/yolo-m2/data/visdrone`（WSL内，ext4快）
- 钢珠数据：`/root/yolo-m2/steelball_data`（train1555+val388）
- 训练输出：`E:\yolo-visdrone\runs\yolo26\yolo26s_full2`
